module.exports = {
  reactStrictMode: true,
  images: {
    domains: ["assets.example.com"],
    loader: "akamai",
    path: "",
  },
};

/**
 * @type {import('next').NextConfig}
 */
 const nextConfig = {
  /* config options here */
}

module.exports = nextConfig